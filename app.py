import streamlit as st
from services.store import ensure_dirs
from utils.ui import light_card

# 根页面：用于导航介绍与快速引导（多页入口在 pages/ 目录）
st.set_page_config(page_title="AI RSS Flow", layout="wide", page_icon="🧬")
ensure_dirs()

st.title("🧬 AI RSS Flow — 多页应用")

st.markdown(
    "- 运行分析：在 ‘运行分析’ 页面选择提示词领域并启动分析流水线，生成 JSON + Markdown 报告；\n"
    "- 历史报告：浏览以往的报告、筛选文章、导出/保存 Markdown；\n"
    "- 提示词与配置：管理提示词组，查看/配置 st.secrets 示例。"
)

light_card(
    "使用提示",
    "1. 首次使用请前往 ‘提示词与配置’ 页面，按照示例配置 .streamlit/secrets.toml。\n\n"
    "2. 完成配置后，在 ‘运行分析’ 页面点击 ‘立即运行’。支持自动将报告文件提交到 Git。\n\n"
    "3. 在 ‘历史报告’ 页面查看与导出。",
)
